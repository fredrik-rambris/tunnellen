{% extends "layout.peb" %}
{% block content %}
<h1 class="h3 mb-3">{{ heading }}</h1>
<div>{{ bodyHtml|raw }}</div>
<p class="mt-3"><a href="{{ backHref }}">&larr; {{ backLabel }}</a></p>
{% endblock %}
